[build]
  publish = "."
  functions = "netlify/functions"

# Vue rewrite experiment: SPA build only on this branch. Other branches keep publish = "." above.
[context."vue-rewrite-experiment"]
  command = "npm run build:vue"
  publish = "dist"